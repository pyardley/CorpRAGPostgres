# Python 3.11 or 3.12 required.
# (Some downstream LangChain packages don't support 3.13+ yet.)

# ── Core framework ─────────────────────────────────────────────────────────
streamlit>=1.40.0,<2.0
python-dotenv>=1.0.1
loguru>=0.7.2
pydantic-settings>=2.6.1,<3.0
pydantic>=2.9.2,<3.0
tenacity>=9.0.0,<10.0

# ── LangChain ecosystem ────────────────────────────────────────────────────
langchain>=0.3.7,<0.4
langchain-community>=0.3.7,<0.4
langchain-openai>=0.2.12,<0.3
langchain-anthropic>=0.2.4,<0.3
langchain-huggingface>=0.1.2,<0.2

# ── Vector store: PostgreSQL + pgvector ────────────────────────────────────
# Install pgvector at the database level (CREATE EXTENSION vector). Supabase
# and Neon ship it pre-installed; for local Postgres see README.
pgvector>=0.3.6,<0.4

# ── Embeddings ─────────────────────────────────────────────────────────────
openai>=1.57.0,<2.0
tiktoken>=0.8.0
sentence-transformers>=3.3.0

# ── LLM clients ────────────────────────────────────────────────────────────
anthropic>=0.40.0,<1.0

# ── Atlassian (Jira + Confluence) ──────────────────────────────────────────
atlassian-python-api>=3.41.0

# ── GitHub (Git source) ────────────────────────────────────────────────────
PyGithub>=2.3.0

# ── Database (app + vector store, both Postgres) ───────────────────────────
sqlalchemy>=2.0.36,<3.0
# psycopg v3 — the modern Postgres driver. `[binary]` ships pre-built
# wheels so no compiler is needed.
psycopg[binary]>=3.2.3,<4.0
alembic>=1.14.0,<2.0

# ── SQL Server driver (only needed for the SQL Server ingestor) ────────────
pyodbc>=5.1.0

# ── Security ───────────────────────────────────────────────────────────────
cryptography>=43.0.0
bcrypt>=4.2.0

# ── HTTP ───────────────────────────────────────────────────────────────────
httpx>=0.28.0
requests>=2.32.0

# ── MCP server (hybrid RAG + live SQL tools) ───────────────────────────────
# The MCP server is a small FastAPI/uvicorn process that the Streamlit app
# spawns on startup (see app/mcp_manager.py). sqlparse gives us robust
# tokenisation for the read-only SELECT validator in
# mcp_server/tools/sql_tools.py.
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
sqlparse>=0.5.1,<1.0

# ── HTML/content parsing ───────────────────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.3.0
markdownify>=0.14.0

# ── Utilities ──────────────────────────────────────────────────────────────
python-dateutil>=2.9.0
